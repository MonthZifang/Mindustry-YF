### 网络与 UDP/TCP

- [x] 修复 TCP 分片发送时部分写入导致的字节丢失。
- [x] UDP 超过约 1200 字节时自动转为 TCP 长度帧。
- [x] 保留小 UDP 包的低延迟传输方式。
- [x] 修复超大 TCP/UDP 对象固定 64 KiB 缓冲区溢出。
- [x] 增加最大可扩展帧大小限制，避免无限分配内存。
- [x] 关闭流量整形后立即解除所有在线连接的整形状态。
- [x] 流量策略保存到 `config/yzf/config/traffic-policy.json`。
- [x] 服务端启动时自动加载流量策略。
- [x] 没有核心网络模块时自动禁止 `fullControl`，避免全服丢包。
- [x] 网关 HTTP 响应增加 `Cache-Control: no-store`。
- [x] Dashboard 返回正确的 HTML MIME 类型。
- [x] 网关请求体限制为 2 MiB，超限返回 HTTP 413。

### 本地 JSON 数据库

- [x] 分类名和键名统一规范化。
- [x] 修复带空格键名无法正确删除的问题。
- [x] 单条记录改为临时文件写入后原子替换。
- [x] 导入 JSON 前先完成对象和分类结构校验。
- [x] 无效导入不会清空现有数据。
- [x] 玩家本地数据文件改为原子写入。
- [x] 数据库注册表改为原子写入。

### SQLite/MySQL/MariaDB/PostgreSQL

- [x] 玩家 SQL 数据保存改为事务提交。
- [x] 玩家 SQL 保存失败自动回滚。
- [x] 玩家数据 `clear()` 同步删除 SQL 表记录。
- [x] KV 数据库导入改为事务性清空和导入。
- [x] KV 导入失败自动回滚，避免部分数据落库。
- [x] 增加 PostgreSQL 专用 `text` 类型。
- [x] 移除 PostgreSQL 不支持的 `on update current_timestamp`。
- [x] PostgreSQL 使用 `on conflict ... do update`。
- [x] 数据库客户端启动失败时不再缓存失败实例。
- [x] 后续访问可以重新尝试启动数据库客户端。

### 远程 JSON 数据库

- [x] 增加真实远程 HTTP 健康检查。
- [x] 非 2xx 响应统一视为失败。
- [x] 分类名和键名使用完整 URL 编码。
- [x] 保留请求和响应 4 MiB 大小限制。
- [x] 禁止自动跟随重定向，避免绕过外部访问策略。
- [x] 远程请求继续使用连接和读取超时。

## 注意事项

- UDP 大包转 TCP 可以降低 IP 分片和突发带宽风险，但不能保证完全消除延迟、回弹或瞬移。
